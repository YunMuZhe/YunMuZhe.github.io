# Java优先级队列
```java
    // 常用API：offer、poll
    PriorityQueue<Map.Entry<Integer, Integer>> priorityQueue = new PriorityQueue<>(Comparator.comparingInt(Map.Entry::getValue));
    
    LinkedList<Integer> queue = new LinkedList<>();

    String s = "test";
    test.toCharArray();
    test.subString(int beginIndex);
    test.substring(int beginIndex, int endIndex);
    test.startWith(int ch);


```